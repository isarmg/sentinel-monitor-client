# sentinel-monitor-client 0.3.10

- ONVIF 响应在接收过程中按大小限制，避免无界读取；配置与运维文档说明当前状态和失败处理。
- 客户端退出时清理其 FFmpeg 子进程，避免媒体进程继续占用设备或输出文件；原始媒体工具错误不进入客户端日志。
- 依赖精确锁定 Foundation Client 0.9.16；Windows 安装器和发行包采用本版版本号。

## 验证

- Rust 测试、格式检查和严格 Clippy 通过；Windows 安装器作者配置检查通过。
